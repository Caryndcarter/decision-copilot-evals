/** Logo periwinkle — demo / brand chrome (not primary actions; use indigo for buttons). */
export function DemoModeStrip() {
  return (
    <div className="border-b border-logo-hover/30 bg-logo px-6 py-2 text-center text-sm font-medium text-white">
      Demo mode
    </div>
  );
}
