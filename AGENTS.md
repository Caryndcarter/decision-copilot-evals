# Agent notes

## Cursor Cloud specific instructions

- **Authorship influence · synthesizer behavior** is a findings cut, not a retag. Public label is **Synthesizer Behavior**; stored study id, route, and snapshot stay `authorship-budget-conditions`. Civitas July 27 (`db9445cf-ef02-5740-b69f-d34a1194e04a`) stays `harness_kind: civitas-replication`. Authorship includes it by batch id (see `AUTHORSHIP_INFLUENCE_INCLUDE_BATCH_IDS` in `packages/nextjs/lib/harness-meta.ts`). Do not rewrite stored kind to `multi-demo-authorship`.
- Those Atlas batches belong to the harness owner, not the VM demo user. Live authorship rollup will be empty for `demo@example.com`. The committed table on **Study findings → Authorship influence → Synthesizer Behavior** (`packages/nextjs/data/authorship-budget-conditions.json`) is the source everyone can see.
- Primary UI labels: “Authorship influence · synthesizer behavior”, scenario “Civitas (constrained tokens)”, control “adequate budget (Sol)”. Always name the setting that was sent rather than a mood word for it. On July 27, GPT-5.5 was the only model sent a reasoning setting: **`reasoning_effort: "low"`** on every structured call, with a **4,096** contribution-analysis cap. Anthropic and xAI got 4,096 caps and **no reasoning setting at all** (vendor defaults, which the repo does not record); Gemini’s structured-output client floor made its effective cap **8,192**. The Sol-era control sent `reasoning_effort: "low"` to **every** provider and doubled ChatGPT's cap to **8,192**. The other three got 16,384 as reasoning headroom — that number is provenance for footnotes only; keep it off the cards, since showing 8,192 next to 16,384 reads as if ChatGPT were shortchanged. Treat this as a descriptive two-condition comparison, not a clean token-only experiment; models and case mix also changed. **Headline finding:** self vs peers→ChatGPT gap (~+2.1 on July 27 vs ~+0.1 in the Sol-era control), not Blind-vs-Revealed self deltas. Never use the word “starved” in any copy, comment, identifier, or filename, and do not use bare labels like “low-effort condition” without stating the setting. Keep caveats in methodology footnotes: public copy states what happened and must not pre-emptively rebut a token-only causal claim (“not proof that a token count alone…”) that no reader has made. The story is calibration: ChatGPT’s self-rating stayed at the top while peer judgment tracked the changing quality of its contribution. Constrained think-tank is gpt-5.5 / **Sonnet** (`claude-sonnet-4-6`) / Gemini / Grok 4.3 — do not label that heatmap Fable. Control think-tank is gpt-5.6-sol / **Fable** / Gemini / Grok 4.5.
- Authorship mode display: stored keys stay `open` / `blind` / `reassigned`. `open` displays as **Revealed**. **Blind** is the default product path and the first column/tab on analysis charts; Revealed and Reassigned are alternatives. The committed synthesizer-behavior snapshot includes all three modes (ChatGPT as rater).
- This VM’s `MONGODB_URI` is local (tour runs only). Harness July 27 / Sol-control persist docs live on Atlas (`ATLAS`). Do not retag those batches. The public `/model-studies/results` table is the committed snapshot.
- Replication moral findings stay on the committed Civitas moral snapshot (`packages/nextjs/lib/civitas-moral-display.ts`). Dual-listing July 27 under Studies → Authorship must not remove it from Studies → Replication.
- A second authorship story — **brand favoritism / Grok-label penalty** — has two public wordings of the same snapshot: `/model-studies/findings/brand-favoritism` (short Grok / ChatGPT) and `/model-studies/findings/brand-favoritism-models` (model ids). Do not invent remap cells. Grok was remapped to ChatGPT 15×, Anthropic 15×, Gemini 10× — not a one-off ChatGPT slap. Constrained: grok-4.3 / gpt-5.5 / claude-sonnet-4-6 / gemini-3.6-flash. Adequate: grok-4.5 / gpt-5.6-sol / claude-fable-5 / gemini-3.6-flash.
- `/model-studies` overview cards are intros, not case writeups. One brand-favoritism card: `/model-studies/findings/brand-favoritism`. High-count story stays `/30`. Paragraph 2 uses 1–4 averages (3.58 / 3.03 / 3.93), not `/40` or `/15`. Why-it-matters may name grok-4.5 with gpt-5.6-sol, claude-fable-5, and gemini-3.6-flash. Do not say “handicap.” Do not use Civitas on these marketing stories. The card visual is **14/30 named → 23/30 remapped**.
- `/demo/result` (product tour Decision Briefs) has an on-page guide (`DemoBriefGuide`) that points at the model menu, collapsed analysis sections, a **canned right-rail chat replay**, then the Unified Brief CTA. `/demo/unified` has a short guide on the same chat rail. Replays are fixtures (`demo-chat-script.ts`) — never call chat APIs from the tour. Do not say “dogfood” in tour copy. Guide dismisses into sessionStorage (`dc-demo-brief-guide` / `dc-demo-unified-guide`).
- Demo Decision Brief and Unified Brief stay short on purpose. Label them with `DEMO_BRIEF_EXCERPT_NOTE` on the shared toolbar — do not fake a fade-out or imply the rest of the document is just off-screen.
